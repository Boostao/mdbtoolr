% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mdbtools_tools.R
\name{mdb_queries}
\alias{mdb_queries}
\title{Get Listing Of Queries In An MDB Database}
\usage{
mdb_queries(
  path,
  query = NULL,
  list = TRUE,
  newline = FALSE,
  delimiter = " ",
  as_text = FALSE,
  as_list = TRUE
)
}
\arguments{
\item{path}{Path to \code{.mdb}/\code{.accdb} file.}

\item{query}{Optional query name; when provided, SQL text is returned.}

\item{list}{Logical, equivalent to \code{-L/--list}.}

\item{newline}{Logical, equivalent to \code{-1/--newline}.}

\item{delimiter}{Delimiter equivalent to \code{-d/--delimiter} (default single space).}

\item{as_text}{Logical; when \code{TRUE}, returns delimited scalar text for listings.}

\item{as_list}{Logical; defaults to \code{TRUE}. When \code{TRUE}, returns a named
\code{mdblist} for query SQL outputs.}
}
\value{
Character vector of query names, query SQL text, or delimited scalar text.
}
\description{
\code{mdb-queries} is a utility program distributed with MDB Tools.
It produces a list of queries in the database, and dumps the SQL associated
with a specific query.
}
\details{
This wrapper returns vectors by default and can collapse listings to CLI-like
text with \code{as_text = TRUE}.
}
\examples{
db <- mdbtoolr:::.mdb_example_nwind_path()
if (nzchar(db)) {
  q <- mdb_queries(db)
  head(q)
  q_sql <- mdb_queries(db, query = c("Orders Qry", "Product Sales for 1997"), as_list = TRUE)
  q_sql
}
}
